{"action":"evaluate","args":{"code":"(() => { const boxes = Array.from(document.querySelectorAll('.lic-row input[type=checkbox], .lic-row [role=checkbox], .lic-check input, .lic-check')); const out = boxes.map(b => b.tagName + '.' + String(b.className).slice(0,40)); if (boxes.length >= 2) { boxes[0].click(); boxes[1].click(); } return JSON.stringify({count: boxes.length, out}); })()"},"session":"layout-ic-prototypes"}