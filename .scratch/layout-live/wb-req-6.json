{"action":"evaluate","args":{"code":"(() => { const all = Array.from(document.querySelectorAll('*')).filter(el => el.children.length === 0 || el.childElementCount <= 3); const cand = Array.from(document.querySelectorAll('[class]')).filter(el => typeof el.className === 'string' && /lic-/.test(el.className)); const cls = [...new Set(cand.map(el => el.className.split(' ').filter(c => c.startsWith('lic-')).join(' ')))]; return JSON.stringify(cls); })()"},"session":"layout-ic-prototypes"}