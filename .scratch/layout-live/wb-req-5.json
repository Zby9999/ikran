{"action":"evaluate","args":{"code":"(() => { const rows = Array.from(document.querySelectorAll('[class*=rule-row], [class*=lic-row]')).filter(el => el.textContent.includes('horizontalProjectGallery')); const row = rows[0]; if (!row) return JSON.stringify({err:'no row', classes: Array.from(document.querySelectorAll('div')).slice(0,40).map(d=>d.className).filter(Boolean).slice(0,20)}); row.dispatchEvent(new MouseEvent('mouseover',{bubbles:true})); return JSON.stringify({hovered: row.className.slice(0,80)}); })()"},"session":"layout-ic-prototypes"}